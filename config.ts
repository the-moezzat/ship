
const config = {
  // REQUIRED
  appName: "Shipventures",
  // REQUIRED: a short description of your app for SEO tags (can be overwritten)
  appDescription:
    "Top-notch software engineers and designers to working together to brings your ideas into reality",
  // REQUIRED (no https://, not trialing slash at the end, just the naked domain)
  domainName: "shipventures.io",
}

export default config;
